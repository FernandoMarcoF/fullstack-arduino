// Definir el tipo para los datos del usuario

export interface Usuario {
  id?: string;
  numSerial: string;
  usuario: string;
  telefono: string;
  fechaAlta: string;
  email: string;
  status: string;
}